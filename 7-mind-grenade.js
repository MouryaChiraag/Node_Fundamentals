const num1 = 5;
const num2 = 10;

function addValues() {
    console.log(`the sum is ${num1 + num2}`)
}

addValues()

// no need to write module.exports if you have a function and then call it within the module.
