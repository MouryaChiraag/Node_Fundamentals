// Type	Description
// dependencies	Packages your app needs to run in production (e.g. Express, React, Axios)
// devDependencies	Packages needed only during development, not when the app is deployed (e.g. nodemon, eslint, prettier)


// const _ = require('lodash');

// const items = [1, [2, [3, [4]]]]

// const newItems = _.flattenDeep(items);
// console.log(newItems);
// console.log("Hello World")